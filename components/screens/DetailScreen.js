import SingleContainer from "../containers/SingleContainer"
const DetailScreen = ({ navigation, route }) => (
  <SingleContainer navigation={navigation} route={route} />
)

export default DetailScreen